
def get_server_port () :
    PORT = 4455
    return (PORT)

def get_server_addr (IP, PORT) :
    ADDR = (IP, PORT)
    return (ADDR)

def get_format () :
    FORMAT = "utf-8"
    return (FORMAT)

def get_size () :
    SIZE = 1024
    return (SIZE)

def get_client_folder () :
    client_folder = input("Enter the sending folder :")
    return (client_folder)

def get_server_folder () :
    server_folder = input("Enter the receiving folder :")
    return (server_folder)

